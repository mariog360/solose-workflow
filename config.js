/* =====================================================================
   Credenciales públicas de Supabase.
   Reemplaza los dos valores con los de tu proyecto:
     Supabase Dashboard > Project Settings > API
       - Project URL         -> url
       - anon / public key   -> anonKey
   La anon key es pública por diseño: no da acceso a nada, porque las
   políticas RLS del esquema exigen estar en la lista de correos.
   ===================================================================== */
window.SOLOSE_CONFIG = {
  url: "",
  anonKey: ""
};
